# 阶段 11 对拍报告：VBA 版 vs Python 移植版

日期：2026-08-22。对拍方式：同一输入三表分别跑 VBA（Excel COM 无头，`RunFullAllocationSilent`）与 Python（`python -m returned_inventory allocate`），五张输出表逐单元格 diff。

## 结论

**6 个场景中 5 个完全一致（五张表零差异），1 个为已核实 VBA 侧缺陷导致的预期分歧（TCXB）。异常分歧 0 处。**

| 场景 | 场景说明 | 分配状态汇总表 | 成功分配明细表 | 数据异常明细表 | 调试日志表 | 运行历史记录表 | 结论 |
|------|---------|:---:|:---:|:---:|:---:|:---:|------|
| TC21 | 回溯成功（SF0016，配置默认） | 一致(2行) | 一致(10行) | 一致(0行) | 一致(0行) | 一致(1行) | 一致 |
| TC24 | E10 回溯超限（SF0028，最大回溯=10/简版日志） | 一致(2行) | 一致(0行) | 一致(0行) | 一致(11行) | 一致(1行) | 一致 |
| TC20 | E08 数量不一致校验失败（SF0060） | 一致(2行) | 一致(0行) | 一致(0行) | 一致(0行) | 一致(1行) | 一致 |
| TC19 | E07 物流单号仅在库存表（SF0059，退单表空） | 一致(1行) | 一致(0行) | 一致(1行) | 一致(0行) | 一致(1行) | 一致 |
| TC36 | E11 QC 碎片库存（SF0036，简版日志） | 一致(1行) | 一致(0行) | 一致(0行) | 一致(0行) | 一致(1行) | 一致 |
| TCXB | 预检测B 碎片场景（S<T<S+minQtyOther，自编） | 一致(1行) | 一致(0行) | 一致(0行) | **分歧(23处)** | **分歧(2处)** | 预期分歧（VBA 侧缺陷） |

比对豁免项：运行历史记录表的「运行时间 / 校验耗时 / 分配耗时 / 总耗时」四列两边天然不同，按列豁免；其余列（含运行编号、错误码分布、回溯次数、配置快照）全部参与比对。

## 预期分歧分析（TCXB，已知偏差①实证）

输入：退单 5 行（00001/00002 各需 10 且仅 ZP 可用，强制需求 S=20；00003~00005 各需 2，minQtyOther=2），库存 ZP=21、QC=5（E08 守恒 26=26）。满足碎片条件 S<T<S+minQtyOther（20<21<22）。

- **Python**（按需求 §4.2.3 完整判定 T≠S 且 T<S+minQtyOther）：预检测B 在进入分配循环前命中，整组标记 E09，调试日志 5 行子类型均为「预检测B（强制竞争库存不足）」，总回溯次数 0。
- **VBA**（modSortFilter.bas 实际只判 `supply < forcedDemand`，21<20 不成立）：漏检，进入分配循环，回溯 1 次后路径穷尽，调试日志子类型为「回溯路径穷尽 / 连带回滚—同SKU未到达行」，总回溯次数 1。

最终业务结论两边相同（汇总表均为「E09 - 分配路径穷尽」，错误码分布均为 `E09:1`），差异仅体现在调试日志细节与回溯计数。**定性：VBA 侧缺陷，非移植 bug，不修改 Python 代码。**

## 已知偏差核实记录

1. **预检测B 碎片漏检**：已用 TCXB 实证，见上节。
2. **E12 中止后追加运行历史**：本次 6 个场景均未命中 E12（输入结构均合法），未触发该偏差；如需覆盖可另造表头错位/缺列工作簿对拍。
3. **TC 冻结文档旧叙述**：实测确认——TC-19 文档预期原因列含括号补充说明「（退单表无对应记录）」，VBA 实际输出为「E07 - 物流单号仅存在于质检库存表」（无括号），Python 与 VBA 实际输出逐字一致。以代码实际输出为准，Python 无需改。
4. **「连带回滚」计数**：TC24 两边历史均为 `E10:1; 连带回滚:1`，一致，无差异。

## 复现步骤

```bash
cd python_port
# 1. 构造输入（生成 data/diff/<case>_input.json 与 <case>_py.xlsx）
./.venv/Scripts/python.exe diff_build_inputs.py
# 2. VBA 侧无头运行（需先有一次性基础工作簿 data/diff/base_vba.xlsm，
#    由「退货入库分配系统 - 测试用完可删.xlsm」复制并用 同步测试工作簿VBA.ps1 同步 17 个模块得到）
powershell -ExecutionPolicy Bypass -File ./diff_run_vba.ps1
# 3. Python 侧运行 + 逐表 diff（退出码：0=无异常分歧，1=存在异常分歧）
./.venv/Scripts/python.exe diff_check.py --run-python
```

脚本：`diff_build_inputs.py`（输入构造）、`diff_run_vba.ps1`（VBA COM 无头运行，内置清空/重置配置/Quit+GC）、`diff_check.py`（比对，内置豁免列与预期分歧标注，可重复运行）。

中间产物全部在 `python_port/data/diff/`：`<case>_vba.xlsm`、`<case>_py.xlsx`、`base_vba.xlsm`、`diff_result.txt`。根目录生产工作簿未触碰。

## 验证回归

- 对拍后 `pytest`：305 passed。
- 残留进程：运行后发现 1 个无窗口 EXCEL 残留（PID 20052，系本次 COM 运行残留），已终止；用户自行打开的 Excel（PID 5072）未受影响。
